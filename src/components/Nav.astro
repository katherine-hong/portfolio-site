---
import NavLinks from './NavLinks.astro';

interface Props {
  showLinks?: boolean;
}
const { showLinks = true } = Astro.props;
---

<nav class="flex items-center justify-between px-8 py-6">
  <a href="/" class="font-bold text-lg">Katherine</a>
  {showLinks && <NavLinks />}
</nav>